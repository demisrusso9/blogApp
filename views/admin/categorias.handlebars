<h2>Lista de Catergorias</h2>
<hr>
<a href="/admin/categorias/add"><button class="btn btn-success">Nova Categoria</button></a>

{{#each categorias}}
<div class="card mt-3">
    <div class="card-body">
        <h4>{{nome}}</h4>
        <small>{{slug}}</small><br>
        <small>{{date}}</small>
    </div>
</div>
{{else}}

{{/each}}